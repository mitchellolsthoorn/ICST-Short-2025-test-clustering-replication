import LinkedList from "../assets/classUnderTest/LinkedList.js";
import chai from "chai";
import chaiAsPromised from "chai-as-promised";

chai.use(chaiAsPromised);
const expect = chai.expect;

describe("LinkedList", () => {
  it("remove assertions for self-returning methods", () => {
    //
  });
});
